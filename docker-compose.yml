version: '3.3'

services:
  cra_app:
    container_name: cra_app
    build: .
    volumes:
      - './:/app'
    ports:
      - 7314:7314
    stdin_open: true
    environment:
      - PORT=7314
    command: npm start